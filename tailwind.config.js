export default {

    theme: {
        extend: {
            fontFamily: {
                sans: ['"DM Sans"', 'sans-serif'],
            }
        }
    },

    plugins: [require('@tailwindcss/line-clamp')],

}